# Resume

Single source of truth: `resume.yaml`.

```bash
# requires: typst, python3-yaml
python3 build.py
```

This regenerates `resume.md` and `resume.pdf` from the YAML via `resume.typ` (Inter fonts in `fonts/`).
